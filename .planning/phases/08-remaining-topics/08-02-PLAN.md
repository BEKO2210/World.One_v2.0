---
phase: 08-remaining-topics
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - detail/topics/crypto_sentiment.js
  - detail/topics/momentum_detail.js
autonomous: true
requirements: [RT-04, MOM-01]

must_haves:
  truths:
    - "User visits detail/?topic=crypto_sentiment and sees 30-day Fear & Greed chart with color zones"
    - "F&G chart bars are colored red for fear, orange for moderate fear, yellow for neutral, green for greed"
    - "User visits detail/?topic=momentum_detail and sees all indicators as mini-cards in a responsive grid"
    - "Each momentum mini-card shows value, trend arrow, score, SVG sparkline, and improvement/decline assessment"
    - "Momentum indicators are aggregated from all 4 category subScores (environment, society, economy, progress)"
  artifacts:
    - path: "detail/topics/crypto_sentiment.js"
      provides: "Crypto Fear & Greed topic module"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
    - path: "detail/topics/momentum_detail.js"
      provides: "World momentum detail topic module with 24 indicator mini-cards"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
  key_links:
    - from: "detail/topics/momentum_detail.js"
      to: "data/processed/world-state.json"
      via: "fetch for subScores.*.indicators aggregation"
      pattern: "subScores|indicators"
    - from: "detail/topics/crypto_sentiment.js"
      to: "hardcoded 30-day data"
      via: "In-script array (no CORS-friendly API)"
      pattern: "FG_HISTORY|fgHistory"
---

<objective>
Implement crypto sentiment and world momentum detail topic modules -- two lighter topics that rely on hardcoded/local data rather than external APIs.

Purpose: Complete the RT-04 and MOM-01 requirements. Crypto sentiment uses hardcoded 30-day Fear & Greed data with color-zoned Chart.js bar chart. Momentum aggregates 24 indicators from world-state.json into a DOM-based mini-card grid with SVG sparklines.
Output: Working detail pages at detail/?topic=crypto_sentiment and detail/?topic=momentum_detail
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-remaining-topics/08-RESEARCH.md

<interfaces>
<!-- Key imports both topic modules need -->

From js/i18n.js:
```javascript
export const i18n = { t(key) { ... }, lang: 'de'|'en' };
```

From js/utils/dom.js:
```javascript
export const DOMUtils = { create(tag, attrs, children), createSVG(tag, attrs), $(sel), ... };
```

From js/utils/data-loader.js:
```javascript
export async function fetchTopicData(topic, apiUrl?);  // Returns { data, tier, age }
```

From js/utils/badge.js:
```javascript
export function createTierBadge(tier, options?);
```

From js/utils/chart-manager.js:
```javascript
export async function ensureChartJs();
export function createChart(canvasId, config);
export const CHART_COLORS;  // { economy: {r,g,b}, momentum: {r,g,b}, ... }
export function toRgba(colorObj, alpha?);
```

From data/processed/world-state.json (momentum indicators):
```javascript
// subScores.environment.indicators[] -- 6 indicators
// subScores.society.indicators[] -- 7 indicators
// subScores.economy.indicators[] -- 5 indicators
// subScores.progress.indicators[] -- 6 indicators
// Each: { name, value, score, trend, source }
// subScores.momentum.indicators[] is EMPTY -- must aggregate from the 4 above
// subScores.momentum: { value: 71.4, positiveCount: 15, negativeCount: 6, totalIndicators: 21 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement crypto sentiment topic module</name>
  <files>detail/topics/crypto_sentiment.js</files>
  <action>
Create detail/topics/crypto_sentiment.js implementing the full topic module contract. Follow the established pattern from currencies.js (same economy category).

**Module structure:**

1. **Imports:** i18n, DOMUtils, createTierBadge, ensureChartJs, createChart, CHART_COLORS (use `economy` category), toRgba

2. **meta:** `{ id: 'crypto_sentiment', titleKey: 'detail.crypto_sentiment.title', category: 'economy', icon: '' }`

3. **Hardcoded 30-day Fear & Greed history** (Pattern 6 -- no CORS-friendly API):
   Create a `FG_HISTORY` constant array with 30 entries, each `{ date: 'YYYY-MM-DD', value: N, label: '...' }`. Use representative values that demonstrate the full range:
   - Mix of Extreme Fear (0-25), Fear (26-45), Neutral (46-55), Greed (56-75), Extreme Greed (76-100)
   - Dates from recent 30-day window (Feb-Mar 2026)
   - Labels: 'Extreme Fear', 'Fear', 'Neutral', 'Greed', 'Extreme Greed'

4. **Color zone function** `_fgColor(value)`:
   - value <= 25: 'rgba(255, 59, 48, 0.7)' -- Extreme Fear (red)
   - value <= 45: 'rgba(255, 149, 0, 0.7)' -- Fear (orange)
   - value <= 55: 'rgba(255, 204, 0, 0.7)' -- Neutral (yellow)
   - value <= 75: 'rgba(52, 199, 89, 0.7)' -- Greed (light green)
   - value > 75: 'rgba(0, 255, 127, 0.7)' -- Extreme Greed (green)

5. **render(blocks):**
   - **Hero:** Show today's F&G value (last entry in FG_HISTORY) as large number with colored background matching the zone. Show label ("Fear", "Greed", etc.) and createTierBadge('static') since data is hardcoded.
   - **Chart block:** Create canvas `fg-history-chart` for 30-day bar chart (lazy via getChartConfigs).
   - **Trend block:** Show a gauge-style visualization using DOM: a horizontal bar from 0-100 with colored segments (red/orange/yellow/green/bright-green) and a pointer at current value. Use DOMUtils.create with inline styles.
   - **Tiles:** Current label + value, 30-day average, 30-day min, 30-day max (computed from FG_HISTORY).
   - **Explanation:** i18n text about what the Fear & Greed Index measures (volatility, momentum, social media, dominance, trends).
   - **Comparison:** Historical context -- Bitcoin halving cycle correlation note.
   - **Sources:** alternative.me Crypto Fear & Greed Index.

6. **getChartConfigs():** Return one chart config for 30-day F&G bar chart:
   - type: 'bar'
   - labels: FG_HISTORY.map(d => d.date) formatted as 'DD.MM'
   - data: FG_HISTORY.map(d => d.value)
   - backgroundColor: FG_HISTORY.map(d => _fgColor(d.value)) -- per-point coloring
   - borderColor: same colors at full opacity
   - x-axis: rotated labels 45deg. y-axis: 0-100 range.
   - Horizontal dashed lines at 25, 50, 75 thresholds using Chart.js annotation plugin alternative: draw as datasets with type 'line' and no points (pointRadius: 0), or use scales grid customization.

7. **cleanup():** Null _chartData module state.

**Important:** Do NOT try to fetch from alternative.me API (no CORS headers). All data is hardcoded per research recommendation.
  </action>
  <verify>
    <automated>node -e "import('./detail/topics/crypto_sentiment.js').then(m => { const ok = m.meta?.id === 'crypto_sentiment' && typeof m.render === 'function' && typeof m.getChartConfigs === 'function' && typeof m.cleanup === 'function'; console.log('Crypto sentiment module:', ok ? 'PASS' : 'FAIL'); if(!ok) process.exit(1); }).catch(e => { console.error(e.message); process.exit(1); });"</automated>
  </verify>
  <done>Crypto sentiment topic module at detail/topics/crypto_sentiment.js with full contract, 30-day F&G history hardcoded, per-point color-zoned bar chart, gauge visualization, tiles with stats</done>
</task>

<task type="auto">
  <name>Task 2: Implement momentum detail topic module</name>
  <files>detail/topics/momentum_detail.js</files>
  <action>
Create detail/topics/momentum_detail.js implementing the full topic module contract. Follow the inequality.js wealth-grid pattern for DOM-based card layout, and weather.js pattern for SVG sparklines.

**Module structure:**

1. **Imports:** i18n, DOMUtils, fetchTopicData (not needed -- use direct fetch), createTierBadge, CHART_COLORS, toRgba. Do NOT import ensureChartJs or createChart -- this module uses zero Canvas charts.

2. **meta:** `{ id: 'momentum_detail', titleKey: 'detail.momentum_detail.title', category: 'momentum', icon: '' }`

3. **Sparkline function** `_createSparkline(trend, color, width=80, height=24)`:
   Since world-state.json only has trend direction (not historical data), generate representative sparkline shapes:
   - 'improving': 10 points on a gentle upward slope with slight noise
   - 'declining': 10 points on a gentle downward slope with slight noise
   - 'stable': 10 points oscillating around center with small amplitude
   Use DOMUtils.createSVG('svg') with DOMUtils.createSVG('polyline'), same pattern as weather.js _createSparkline but with synthetic data points based on trend direction.

4. **Assessment function** `_assessIndicator(score, trend)`:
   Returns { label, color } based on score + trend combo:
   - score >= 70 AND trend === 'improving': { label: 'Improving', color: '#34c759' } (green)
   - score >= 50 AND trend !== 'declining': { label: 'Stable', color: '#ffcc00' } (yellow)
   - score < 50 OR trend === 'declining': { label: 'Declining', color: '#ff3b30' } (red)

5. **Category mapping** for colors and i18n:
   - environment: { color: '#34c759', key: 'detail.momentum_detail.categoryEnv' }
   - society: { color: '#5ac8fa', key: 'detail.momentum_detail.categorySoc' }
   - economy: { color: '#ff9500', key: 'detail.momentum_detail.categoryEcon' }
   - progress: { color: '#af52de', key: 'detail.momentum_detail.categoryProg' }

6. **render(blocks):**
   - **Data loading:** Fetch world-state.json directly via `fetch(basePath + 'data/processed/world-state.json')` (use _basePath() pattern from data-loader.js, checking window.location.pathname for '/detail/' prefix to add '../'). Parse subScores. Aggregate indicators from environment.indicators + society.indicators + economy.indicators + progress.indicators. Each indicator gets a `category` field attached.
   - **CRITICAL (Pitfall 3 from research):** Do NOT use subScores.momentum.indicators (it is empty []). Aggregate from the 4 named categories above to get ~24 indicators.
   - **Hero:** Show momentum score (subScores.momentum.value), positiveCount/negativeCount/totalIndicators. Use createTierBadge('live') since data comes from processed file.
   - **Chart block (used as card grid):** Build responsive CSS grid of mini-cards. For each indicator:
     - Card container: padding 12px, background rgba(255,255,255,0.04), borderRadius 8px, borderLeft 3px solid with category color.
     - Inside card: indicator name (bold), value (as-is from data), trend arrow (unicode: improving='&#9650;' green, declining='&#9660;' red, stable='&#9654;' yellow), score as small badge, SVG sparkline from _createSparkline(trend, categoryColor), assessment badge from _assessIndicator.
   - Grid layout: CSS grid with `grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))` and gap 12px.
   - **Trend block:** Summary statistics: count of improving/declining/stable indicators as colored horizontal bar chart (DOM bars, not Chart.js). Shows proportional segments like a stacked bar.
   - **Tiles:** Category summaries -- one tile per category showing its score and indicator count.
   - **Explanation:** i18n text about how the momentum score aggregates 24 global indicators.
   - **Comparison:** Show environment vs economy contrast -- which areas improving/declining.
   - **Sources:** World.One aggregation from World Bank, NOAA, UNHCR, ACLED, Freedom House, etc.

7. **getChartConfigs():** Return empty array `[]` -- no Chart.js canvases used. All visualizations are DOM/SVG.

8. **cleanup():** Null module state (_indicators, _momentumData).

**Key pattern:** This is a DOM-only module like the weather city cards. No ensureChartJs() call. All 24 cards render as lightweight DOM elements with inline SVG sparklines.
  </action>
  <verify>
    <automated>node -e "import('./detail/topics/momentum_detail.js').then(m => { const ok = m.meta?.id === 'momentum_detail' && typeof m.render === 'function' && typeof m.getChartConfigs === 'function' && typeof m.cleanup === 'function'; console.log('Momentum module:', ok ? 'PASS' : 'FAIL'); if(!ok) process.exit(1); }).catch(e => { console.error(e.message); process.exit(1); });"</automated>
  </verify>
  <done>Momentum detail topic module at detail/topics/momentum_detail.js with full contract, 24 indicator mini-cards aggregated from 4 subScore categories, SVG sparklines by trend direction, assessment badges, responsive grid layout, zero Chart.js instances</done>
</task>

</tasks>

<verification>
- `detail/?topic=crypto_sentiment` loads with F&G hero value and colored 30-day bar chart
- Bar chart has per-point coloring: red for low values, green for high values
- `detail/?topic=momentum_detail` loads with momentum score hero and 24 mini-cards in grid
- Each mini-card shows name, value, trend arrow, sparkline SVG, and assessment badge
- Mini-cards are aggregated from environment + society + economy + progress (not empty momentum.indicators)
- No console errors on either topic page
</verification>

<success_criteria>
1. detail/topics/crypto_sentiment.js implements full topic contract with hardcoded 30-day F&G data
2. F&G bar chart has per-point color zones (red/orange/yellow/green by value threshold)
3. detail/topics/momentum_detail.js implements full topic contract with DOM-only card grid
4. Momentum aggregates ~24 indicators from 4 subScore categories (environment, society, economy, progress)
5. Each card has SVG sparkline generated from trend direction, plus assessment badge
6. Neither module fetches external APIs that lack CORS headers
</success_criteria>

<output>
After completion, create `.planning/phases/08-remaining-topics/08-02-SUMMARY.md`
</output>
