---
phase: 02-detail-page-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - detail/index.html
  - css/detail.css
  - js/i18n.js
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-04
  - DETAIL-07
  - DETAIL-08
  - DETAIL-10
  - DETAIL-12

must_haves:
  truths:
    - "detail/index.html loads in the browser without 404 or console errors when served from the detail/ subdirectory"
    - "7 layout blocks are present in DOM in order: hero, chart, trend, tiles, explanation, comparison, sources"
    - "Skeleton shimmer animation plays on every layout block before content loads"
    - "Print preview shows white background, black text, hidden navigation, no animations"
    - "All detail.* i18n keys exist in both DE and EN translation objects"
    - "CSS custom properties from core.css are reused -- glass-morphism, spacing, typography match main page"
  artifacts:
    - path: "detail/index.html"
      provides: "Detail page HTML shell with 7 layout blocks, nav bar, error state, toast, back-to-top"
      contains: "detail-hero"
    - path: "css/detail.css"
      provides: "Layout grid, skeleton loaders, print CSS, nav styles, time range styles, error/toast styles"
      contains: "@media print"
    - path: "js/i18n.js"
      provides: "detail.* translation keys for all UI chrome in DE and EN"
      contains: "detail.back"
  key_links:
    - from: "detail/index.html"
      to: "css/core.css"
      via: "link rel=stylesheet href=../css/core.css"
      pattern: "href.*core\\.css"
    - from: "detail/index.html"
      to: "css/detail.css"
      via: "link rel=stylesheet href=../css/detail.css"
      pattern: "href.*detail\\.css"
    - from: "detail/index.html"
      to: "detail/detail-app.js"
      via: "script type=module src=./detail-app.js"
      pattern: "detail-app\\.js"
---

<objective>
Create the static foundation of the detail page system: the HTML shell with 7 layout blocks, skeleton loaders, navigation structure, and all CSS styles including print stylesheet. Add all detail.* i18n translation keys to the existing i18n system.

Purpose: This is the structural skeleton that the detail-app.js (Plan 02) will bring to life. Without this HTML/CSS/i18n foundation, the JS router has no DOM to render into and no translations to use.

Output: detail/index.html, css/detail.css, updated js/i18n.js
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detail-page-shell/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should reference these for CSS variable names and i18n patterns. -->

From css/core.css (CSS custom properties to reuse):
```css
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #12121a;
  --text-primary: #f0f0f5;
  --text-secondary: #8e8e93;
  --text-muted: #5a5a65;
  --glass-bg: rgba(255, 255, 255, 0.04);
  --glass-bg-hover: rgba(255, 255, 255, 0.08);
  --glass-border: rgba(255, 255, 255, 0.08);
  --glass-border-hover: rgba(255, 255, 255, 0.15);
  --glass-blur: 20px;
  --space-xs through --space-2xl (clamp-based responsive);
  --text-hero through --text-micro (clamp-based responsive);
  --max-width: 1200px;
  --duration-fast: 200ms;
  --duration-normal: 400ms;
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
}
```

From js/i18n.js (translation pattern -- add keys after badge.* section):
```javascript
// DE section: badge keys end at line ~374
// EN section: badge keys end at line ~745
// New keys go as: 'detail.keyName': 'Translation value'
// Interpolation: 'detail.shareText': 'Schau dir {topic} an auf World.One'
// i18n.t('key', { param: value }) for interpolation
```

From js/utils/dom.js (DOMUtils.observe for IntersectionObserver):
```javascript
observe(elements, callback, options = {}) {
  // accepts string selector, Element, or iterable
  // returns IntersectionObserver instance
  // default: threshold: 0.1, rootMargin: '0px'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create detail/index.html shell with 7 layout blocks and navigation</name>
  <files>detail/index.html</files>
  <action>
Create the detail page HTML shell at `detail/index.html`. This file is served from the `detail/` subdirectory, so ALL paths to root files must use `../` prefix.

Structure (in order):
1. `<head>`: charset, viewport, theme-color (#0a0a0f), title "World.One - Detail"
   - Link `../css/core.css` (shared design system)
   - Link `../css/components.css` (shared components including badges)
   - Link `../css/detail.css` (detail-specific styles, created in Task 2)

2. Fixed navigation bar `<nav class="detail-nav">`:
   - Back link: `<a href="../index.html" class="detail-nav__back" data-i18n-aria="detail.back">` with inline SVG left-arrow (24x24, stroke currentColor, 2px stroke-width)
   - Breadcrumb: `<div class="detail-nav__breadcrumb">` with "World.One" span (data-i18n="detail.breadcrumb.home"), separator "/", and `<span id="detail-breadcrumb-topic">` for dynamic topic name
   - Actions group: `<div class="detail-nav__actions">`
     - Share button: `<button id="detail-share" class="detail-nav__btn" data-i18n-aria="detail.share">` with inline SVG share icon
     - Language toggle: `<button id="detail-lang-toggle" class="detail-nav__btn detail-nav__btn--lang">` with `<span class="detail-nav__lang-label">EN</span>`

3. Main content `<main class="detail-page" id="detail-main">` with 7 sections in order:
   - `<section class="detail-block detail-block--skeleton" id="detail-hero" data-block="hero">`
   - `<section class="detail-block detail-block--skeleton" id="detail-chart" data-block="chart">`
   - `<section class="detail-block detail-block--skeleton" id="detail-trend" data-block="trend">`
   - `<section class="detail-block detail-block--skeleton" id="detail-tiles" data-block="tiles">`
   - `<section class="detail-block detail-block--skeleton" id="detail-explanation" data-block="explanation">`
   - `<section class="detail-block detail-block--skeleton" id="detail-comparison" data-block="comparison">`
   - `<footer class="detail-block detail-block--skeleton" id="detail-sources" data-block="sources">`
   Each section gets an `aria-label` attribute for accessibility.

4. Back-to-top button: `<button class="detail-btt" id="detail-btt" data-i18n-aria="detail.backToTop" hidden>` with inline SVG up-arrow

5. Toast notification: `<div class="detail-toast" id="detail-toast" role="status" aria-live="polite" hidden>`

6. Error state container (hidden by default): `<div class="detail-error" id="detail-error" hidden>` with error card containing title (data-i18n="detail.error.title"), message span (id="detail-error-message"), and back-home link (data-i18n="detail.error.backHome", href="../index.html")

7. Script tag: `<script type="module" src="./detail-app.js"></script>`

Set `<html lang="de" data-lang="de">` to match main page convention.

SVG icons should be minimal inline SVGs (not external files). Back arrow: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>`. Share icon: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 12v8a2 2 0 002 2h12a2 2 0 002-2v-8M16 6l-4-4-4 4M12 2v13"/></svg>`. Up arrow: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 19V5M5 12l7-7 7 7"/></svg>`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('detail/index.html','utf8'); const ids=['detail-hero','detail-chart','detail-trend','detail-tiles','detail-explanation','detail-comparison','detail-sources','detail-nav','detail-main','detail-error','detail-btt','detail-toast','detail-share','detail-lang-toggle','detail-breadcrumb-topic']; const missing=ids.filter(id=>!h.includes('id=\"'+id+'\"')); if(missing.length){console.error('Missing IDs:',missing);process.exit(1)} if(!h.includes('../css/core.css')){console.error('Missing core.css link');process.exit(1)} if(!h.includes('./detail-app.js')){console.error('Missing detail-app.js script');process.exit(1)} console.log('All 15 required IDs present, CSS/JS links correct')"</automated>
  </verify>
  <done>detail/index.html exists with all 7 layout blocks, nav bar, error state, toast, BTT button, correct relative paths to CSS/JS, and all required element IDs</done>
</task>

<task type="auto">
  <name>Task 2: Create css/detail.css with layout, skeleton, print, nav, and component styles</name>
  <files>css/detail.css</files>
  <action>
Create `css/detail.css` with ALL detail page styles. Use CSS custom properties from core.css exclusively (no hardcoded colors). Follow BEM naming convention. Sections:

**1. Detail Nav (.detail-nav):**
- Fixed position, top 0, full width, z-index 1000
- Glass-morphism background: `background: var(--glass-bg); backdrop-filter: blur(var(--glass-blur)); border-bottom: 1px solid var(--glass-border))`
- Height ~56px, flex row, align-items center, padding 0 var(--space-md)
- `.detail-nav__back`: flex center, 40x40px, border-radius 50%, transition background var(--duration-fast), hover bg var(--glass-bg-hover), text-decoration none, color var(--text-primary)
- `.detail-nav__breadcrumb`: flex row, gap var(--space-xs), font-size var(--text-label), color var(--text-secondary)
- `.detail-nav__separator`: color var(--text-muted)
- `.detail-nav__actions`: margin-left auto, flex row, gap var(--space-sm)
- `.detail-nav__btn`: 40x40px, border-radius 50%, background transparent, border 1px solid var(--glass-border), color var(--text-primary), cursor pointer, transition all var(--duration-fast), hover border-color var(--glass-border-hover) and bg var(--glass-bg-hover)
- `.detail-nav__btn--lang`: width auto, padding 0 12px, font-size var(--text-label), font-weight 600

**2. Detail Page Layout (.detail-page):**
- Max-width var(--max-width), margin 0 auto, padding: 80px var(--space-md) var(--space-xl) (80px top for fixed nav clearance)
- Display flex, flex-direction column, gap var(--space-lg)

**3. Detail Blocks (.detail-block):**
- Background: var(--glass-bg), border: 1px solid var(--glass-border), border-radius: 16px
- Padding: var(--space-lg)
- Position relative (for skeleton pseudo-element)

**4. Skeleton Loaders (.detail-block--skeleton):**
- overflow: hidden
- Min-height per block type: #detail-hero 300px, #detail-chart and #detail-trend 400px, others 200px
- `::after` pseudo-element: absolute, inset var(--space-md), border-radius 12px
- Background: linear-gradient(90deg, rgba(255,255,255,0.02) 25%, rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.02) 75%)
- background-size: 200% 100%
- animation: skeletonShimmer 1.8s ease-in-out infinite
- `.detail-block--loaded`: removes skeleton, no ::after

**5. @keyframes skeletonShimmer:**
- 0%: background-position 200% 0
- 100%: background-position -200% 0

**6. Back-to-Top (.detail-btt):**
- Fixed, bottom var(--space-lg), right var(--space-lg), z-index 999
- 48x48px, border-radius 50%, background var(--glass-bg), border 1px solid var(--glass-border), backdrop-filter blur(var(--glass-blur))
- Color var(--text-primary), cursor pointer
- Transition: opacity var(--duration-normal), transform var(--duration-normal)
- `[hidden]`: opacity 0, pointer-events none, transform translateY(20px)
- Show state (no hidden attr): opacity 1, transform translateY(0)

**7. Toast (.detail-toast):**
- Fixed, bottom var(--space-xl), left 50%, transform translateX(-50%), z-index 1001
- Background var(--bg-secondary), border 1px solid var(--glass-border), border-radius 8px
- Padding var(--space-xs) var(--space-md), font-size var(--text-label), color var(--text-primary)
- Transition: opacity var(--duration-normal), transform var(--duration-normal)
- `[hidden]`: opacity 0, pointer-events none

**8. Error State (.detail-error):**
- Display flex, align-items center, justify-content center, min-height 60vh
- `.detail-error__card`: background var(--glass-bg), border 1px solid var(--glass-border), border-radius 16px, padding var(--space-xl), text-align center, max-width 400px
- `.detail-error__title`: font-size var(--text-sub), color var(--text-primary), margin-bottom var(--space-sm)
- `.detail-error__message`: font-size var(--text-body), color var(--text-secondary), margin-bottom var(--space-lg)
- `.detail-error__btn`: display inline-block, padding var(--space-xs) var(--space-md), background var(--glass-bg-hover), border 1px solid var(--glass-border), border-radius 8px, color var(--text-primary), text-decoration none, transition all var(--duration-fast), hover border-color var(--glass-border-hover)

**9. Time Range Selector (.time-range):**
- Display flex, gap 4px, background var(--glass-bg), border-radius 8px, padding 4px, border 1px solid var(--glass-border)
- `.time-range__btn`: padding var(--space-xs) var(--space-sm), border-radius 6px, border none, background transparent, color var(--text-secondary), cursor pointer, font-size var(--text-label), transition all var(--duration-fast)
- `.time-range__btn--active`: background var(--glass-bg-hover), color var(--text-primary), font-weight 600
- `.time-range__btn:hover:not(.time-range__btn--active)`: color var(--text-primary)

**10. Print Stylesheet (@media print):**
- Override :root variables: --bg-primary: #fff, --bg-secondary: #f5f5f5, --text-primary: #000, --text-secondary: #333, --text-muted: #666, --glass-bg: transparent, --glass-border: #ddd
- body: background #fff, color #000
- Hide: .detail-nav, .detail-btt, .detail-toast, .time-range with `display: none !important`
- Kill all animations: `*, *::before, *::after { animation: none !important; transition: none !important; }`
- Canvas: `print-color-adjust: exact; -webkit-print-color-adjust: exact`
- .detail-block: `break-inside: avoid; page-break-inside: avoid; margin-bottom: 1rem; border-color: #ddd`
- Remove glass effect on blocks: background transparent for print clarity

**11. Responsive adjustments:**
- Mobile (<640px): .detail-nav padding reduced, .detail-block padding var(--space-md), .detail-nav__breadcrumb hide separator and home on very small screens if needed
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('css/detail.css','utf8'); const checks=['detail-nav','detail-block--skeleton','skeletonShimmer','detail-btt','detail-toast','detail-error','time-range','@media print','detail-page']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.error('Missing CSS sections:',missing);process.exit(1)} console.log('All 9 required CSS sections present')"</automated>
  </verify>
  <done>css/detail.css contains all layout, skeleton, print, navigation, error, toast, time range, and back-to-top styles using CSS custom properties from core.css with BEM naming convention</done>
</task>

<task type="auto">
  <name>Task 3: Add detail.* i18n translation keys to js/i18n.js</name>
  <files>js/i18n.js</files>
  <action>
Add all detail page translation keys to the existing js/i18n.js file. Insert a new `// --- Detail Page ---` section AFTER the existing `// --- Data Badges ---` section in BOTH the DE and EN translation objects.

**DE keys to add (after line ~374, after badge.staleWarning):**
```
// --- Detail Page ---
'detail.back': 'Zurueck',
'detail.backToTop': 'Nach oben scrollen',
'detail.breadcrumb.home': 'World.One',
'detail.share': 'Teilen',
'detail.shareText': 'Schau dir {topic} an auf World.One',
'detail.copied': 'Link kopiert!',
'detail.error.title': 'Thema nicht gefunden',
'detail.error.invalidTopic': 'Das Thema "{topic}" existiert nicht.',
'detail.error.loadFailed': 'Das Thema "{topic}" konnte nicht geladen werden.',
'detail.error.backHome': 'Zurueck zur Startseite',
'detail.range.label': 'Zeitraum waehlen',
'detail.range.1y': '1J',
'detail.range.5y': '5J',
'detail.range.20y': '20J',
'detail.range.max': 'Max',
'detail.langToggle': 'Sprache wechseln',
'detail.hero': 'Hauptindikator',
'detail.chart': 'Hauptdiagramm',
'detail.trend': 'Historischer Trend',
'detail.tiles': 'Kontext',
'detail.explanation': 'Erklaerung',
'detail.comparison': 'Vergleich',
'detail.sources': 'Quellen',
'detail.loading': 'Lade Daten...',
```

**EN keys to add (after line ~745, after badge.staleWarning):**
```
// --- Detail Page ---
'detail.back': 'Back',
'detail.backToTop': 'Scroll to top',
'detail.breadcrumb.home': 'World.One',
'detail.share': 'Share',
'detail.shareText': 'Check out {topic} on World.One',
'detail.copied': 'Link copied!',
'detail.error.title': 'Topic not found',
'detail.error.invalidTopic': 'The topic "{topic}" does not exist.',
'detail.error.loadFailed': 'The topic "{topic}" could not be loaded.',
'detail.error.backHome': 'Back to homepage',
'detail.range.label': 'Select time range',
'detail.range.1y': '1Y',
'detail.range.5y': '5Y',
'detail.range.20y': '20Y',
'detail.range.max': 'Max',
'detail.langToggle': 'Switch language',
'detail.hero': 'Key indicator',
'detail.chart': 'Primary chart',
'detail.trend': 'Historical trend',
'detail.tiles': 'Context',
'detail.explanation': 'Explanation',
'detail.comparison': 'Comparison',
'detail.sources': 'Sources',
'detail.loading': 'Loading data...',
```

IMPORTANT: Read js/i18n.js first to find the exact insertion points. Do NOT overwrite existing keys. Use the same quoting style (single quotes) and indentation (4 spaces) as the existing file. The badge.staleWarning key is the last key before the new section in both DE and EN objects.

Note: Use simple ASCII characters (no umlauts) in German translations to match the existing file pattern. The existing file uses 'ae' for 'ae', 'ue' for 'ue', 'oe' for 'oe'.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('js/i18n.js','utf8'); const keys=['detail.back','detail.backToTop','detail.share','detail.shareText','detail.copied','detail.error.title','detail.error.invalidTopic','detail.error.loadFailed','detail.error.backHome','detail.range.1y','detail.range.5y','detail.range.20y','detail.range.max','detail.breadcrumb.home','detail.langToggle','detail.hero','detail.chart','detail.trend','detail.tiles','detail.explanation','detail.comparison','detail.sources','detail.loading','detail.range.label']; const missing=keys.filter(k=>{const count=(c.match(new RegExp(\"'\"+k.replace('.','\\\\.')+\"'\",  'g'))||[]).length; return count<2}); if(missing.length){console.error('Keys missing or not in both DE+EN:',missing);process.exit(1)} console.log('All 24 detail.* keys present in both DE and EN')"</automated>
  </verify>
  <done>js/i18n.js contains all 24 detail.* translation keys in both the DE and EN translation objects, matching existing code style</done>
</task>

</tasks>

<verification>
1. detail/index.html contains all 7 layout blocks with correct IDs and data-block attributes
2. detail/index.html links to ../css/core.css, ../css/components.css, ../css/detail.css (relative paths from detail/ subdirectory)
3. css/detail.css contains layout, skeleton, print, nav, error, toast, time-range, BTT styles
4. css/detail.css @media print overrides all dark-theme variables to light
5. js/i18n.js has all 24 detail.* keys in both DE and EN
6. No console errors when opening detail/index.html in a browser (CSS loads, JS reference exists)
</verification>

<success_criteria>
- detail/index.html is a valid HTML5 document with 7 skeleton-loading layout blocks
- css/detail.css provides glass-morphism styling matching the main page aesthetic
- Print preview renders white background with black text
- All UI text strings are in the i18n system, not hardcoded
- The page shell is ready for detail-app.js (Plan 02) to bring to life
</success_criteria>

<output>
After completion, create `.planning/phases/02-detail-page-shell/02-01-SUMMARY.md`
</output>
