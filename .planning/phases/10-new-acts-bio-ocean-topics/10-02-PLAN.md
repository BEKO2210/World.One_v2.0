---
phase: 10-new-acts-bio-ocean-topics
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - detail/topics/ocean_temp.js
  - detail/topics/ocean_ph.js
  - detail/topics/ocean_plastic.js
autonomous: true
requirements: [OCEAN-01, OCEAN-02, OCEAN-03]

must_haves:
  truths:
    - "User visits detail/?topic=ocean_temp and sees SST anomaly hero, SST timeline chart (1880-present), SVG heatmap choropleth, and coral bleaching threshold explainer"
    - "User visits detail/?topic=ocean_ph and sees pH value hero, pH trend chart (1850-2025), interactive pH scale visualization, and acidification impact infographic"
    - "User visits detail/?topic=ocean_plastic and sees plastic estimate hero, animated daily counter ticking live, 5 garbage patches SVG map, and decomposition time animation"
    - "All three modules display LIVE/Cache/Static tier badges correctly"
    - "All three modules clean up intervals and choropleth listeners on navigation away"
  artifacts:
    - path: "detail/topics/ocean_temp.js"
      provides: "Ocean temperature topic module (OCEAN-01)"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
      min_lines: 200
    - path: "detail/topics/ocean_ph.js"
      provides: "Ocean pH topic module (OCEAN-02)"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
      min_lines: 200
    - path: "detail/topics/ocean_plastic.js"
      provides: "Ocean plastic topic module (OCEAN-03)"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
      min_lines: 250
  key_links:
    - from: "detail/topics/ocean_temp.js"
      to: "data/cache/ocean.json"
      via: "fetchTopicData('ocean')"
      pattern: "fetchTopicData.*ocean"
    - from: "detail/topics/ocean_temp.js"
      to: "detail/utils/choropleth.js"
      via: "renderChoropleth() for SST heatmap"
      pattern: "renderChoropleth"
    - from: "detail/topics/ocean_plastic.js"
      to: "cleanup()"
      via: "_intervals array tracking setInterval IDs"
      pattern: "_intervals.*push.*setInterval"
---

<objective>
Create three ocean detail topic modules (ocean_temp, ocean_ph, ocean_plastic) following the DETAIL-03 contract with rich visualizations: SST timeline with choropleth, pH trend with scale visualization, and plastic counter with garbage patches SVG map.

Purpose: Users clicking ocean tiles on the main page (Act 13) land on fully functional detail pages with live data, interactive charts, and educational infographics about ocean health.
Output: Three topic module JS files in detail/topics/ providing complete ocean topic coverage.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-new-acts-bio-ocean-topics/10-RESEARCH.md
@.planning/phases/10-new-acts-bio-ocean-topics/10-01-SUMMARY.md

Prior phase context:
- Phase 5 Plan 01 established choropleth pattern and biodiversity module (05-01-SUMMARY.md)
- Phase 4 established topic module pattern with hardcoded data

<interfaces>
<!-- DETAIL-03 topic module contract (every topic module must export these) -->
```javascript
export const meta = { id: string, titleKey: string, category: string, icon: string };
export async function render(blocks) { /* blocks = { hero, chart, trend, tiles, explanation, comparison, sources } */ }
export function getChartConfigs() { return []; }  // or array of { containerId, config }
export function cleanup() { /* clear intervals, null cacheData, call choropleth cleanup */ }
```

<!-- Available utilities -->
```javascript
import { i18n } from '../../js/i18n.js';
import { DOMUtils } from '../../js/utils/dom.js';
import { fetchTopicData } from '../../js/utils/data-loader.js';
import { createTierBadge } from '../../js/utils/badge.js';
import { ensureChartJs, createChart, CHART_COLORS, toRgba } from '../../js/utils/chart-manager.js';
import { renderChoropleth } from '../utils/choropleth.js';
```

<!-- ocean.json cache structure -->
```json
{
  "_meta": { "fetched_at": "...", "source": "cache-pipeline", "version": "1.0" },
  "annual_sst_anomaly": [ { "year": 1880, "anomaly": -0.05 }, ... { "year": 2025, "anomaly": 0.83 } ]
}
```
NOTE: ocean.json has ONLY annual_sst_anomaly. No pH or plastic data in cache.

<!-- Counter pattern from population.js -->
```javascript
const startOfDay = new Date();
startOfDay.setHours(0, 0, 0, 0);
const dailyRate = 23_000_000; // kg/day
const intervalId = setInterval(() => {
  const elapsed = (Date.now() - startOfDay.getTime()) / 86_400_000;
  counterEl.textContent = Math.floor(dailyRate * elapsed).toLocaleString();
}, 1000);
_intervals.push(intervalId);
```

<!-- SVG map pattern from earthquakes.js/conflicts.js -->
```javascript
const svgNS = 'http://www.w3.org/2000/svg';
const svg = document.createElementNS(svgNS, 'svg');
svg.setAttribute('viewBox', '0 0 1000 500');
// Add continent outlines, then positioned markers
```

<!-- Choropleth usage pattern from biodiversity.js -->
```javascript
const { wrapper, cleanup: cleanupMap } = renderChoropleth(container, {
  dataMap: { 'US': 0.7, 'CN': 0.4, ... },  // ISO-2 -> value (0-1 normalized)
  colorFn: (val) => `rgba(255, ${Math.round(255 - val * 200)}, 0, 0.8)`,
  tooltipFn: (iso, val) => `${iso}: ${val.toFixed(2)}`,
  legendItems: [ { color: '#ff0000', label: 'High' }, ... ],
  title: 'Map Title'
});
_choroplethCleanup = cleanupMap;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ocean_temp.js topic module (OCEAN-01) with SST timeline, choropleth, and coral bleaching explainer</name>
  <files>detail/topics/ocean_temp.js</files>
  <action>
Create detail/topics/ocean_temp.js following the DETAIL-03 contract. Module structure:

**Header comment:** World.One 1.0 -- Ocean Temperature Topic Module (OCEAN-01)

**Imports:** i18n, DOMUtils, fetchTopicData, createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba, renderChoropleth

**meta:** id: 'ocean_temp', titleKey: 'detail.ocean_temp.title', category: 'ocean', icon: ''

**Module state:** _intervals = [], _cacheData = null, _choroplethCleanup = null

**Hardcoded data (no cache for regional SST):**
- REGIONAL_SST_ANOMALY: ~30 country ISO-2 codes mapped to normalized 0-1 SST anomaly values for the choropleth. Use approximate regional values based on IPCC AR6: Arctic regions (NO, IS, RU) high ~0.9-1.0, tropical Pacific (PH, ID, AU) medium ~0.5-0.7, temperate (US, CN, JP, DE, FR, GB, BR, IN) ~0.4-0.6, Southern Ocean (NZ, AR, CL) ~0.3-0.5. Include at least 30 countries for good map coverage.
- CORAL_THRESHOLDS: DHW (Degree Heating Weeks) data for the infographic: 0-4 = Bleaching Watch, 4-8 = Bleaching Warning, 8+ = Bleaching Alert, with color codes.

**render(blocks) function:**

1. **Hero block (blocks.hero):** Fetch data via `fetchTopicData('ocean')`. Extract latest SST anomaly from data.annual_sst_anomaly (last element). Display: anomaly value with +/- sign and " C" suffix, colored red/orange based on magnitude. Add tier badge. Show year and source text.

2. **Chart block (blocks.chart):** SST anomaly timeline chart (1880-present). Use ensureChartJs() then createChart(). Line chart with:
   - X-axis: years from annual_sst_anomaly
   - Y-axis: anomaly values
   - Line color: gradient from blue (negative) to red (positive) -- use a single line with CHART_COLORS[0]
   - Fill below line with toRgba(CHART_COLORS[0], 0.2)
   - Annotation line at y=0 (baseline) as dashed gray
   - Title via i18n key

3. **Trend block (blocks.trend):** Context tiles showing key milestones:
   - "Warmest Year" tile: find max anomaly from data, show year + value
   - "Rate of Warming" tile: calculate trend (last 50 years linear slope approximation)
   - "Since Pre-industrial" tile: show latest anomaly vs 1880-1900 average
   All built with DOMUtils.create() (no innerHTML).

4. **Tiles block (blocks.tiles):** Coral bleaching threshold infographic. Create a visual scale using DOM elements:
   - 4 horizontal bars (Watch/Warning/Alert Level 1/Alert Level 2) with DHW ranges
   - Color-coded: yellow, orange, red, dark red
   - Current global DHW indicator positioned on the scale
   Use CSS flexbox layout, not Chart.js.

5. **Explanation block (blocks.explanation):** Short text explaining what SST anomaly means and why it matters (coral bleaching, marine ecosystems, weather patterns). Use i18n keys.

6. **Comparison block (blocks.comparison):** SVG heatmap choropleth showing regional SST anomalies. Use renderChoropleth() with:
   - dataMap: REGIONAL_SST_ANOMALY object
   - colorFn: blue-to-red gradient based on anomaly value (0 = blue #0066ff, 1 = red #ff3333)
   - tooltipFn: shows country ISO + anomaly description
   - legendItems: 4 items from Low to Extreme
   - Store cleanup function in _choroplethCleanup

7. **Sources block (blocks.sources):** List data sources: NOAA Climate at a Glance, NOAA Coral Reef Watch, IPCC AR6 WG1

**getChartConfigs():** Return empty array (chart rendered directly in render() for potential timerangechange support).

**cleanup():** Clear _intervals, null _cacheData, call _choroplethCleanup if set.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('detail/topics/ocean_temp.js','utf8'); console.assert(f.includes('export const meta'),'missing meta'); console.assert(f.includes('export async function render'),'missing render'); console.assert(f.includes('export function getChartConfigs'),'missing getChartConfigs'); console.assert(f.includes('export function cleanup'),'missing cleanup'); console.assert(f.includes(\"fetchTopicData('ocean')\"),'not fetching ocean data'); console.assert(f.includes('renderChoropleth'),'missing choropleth'); console.assert(f.includes('_intervals'),'missing interval tracking'); console.log('ocean_temp.js OK')"</automated>
  </verify>
  <done>detail/?topic=ocean_temp shows SST anomaly hero with tier badge, 1880-present SST timeline chart, warming milestones tiles, coral bleaching threshold infographic, regional SST choropleth map, and sources footer. Cleanup properly disposes intervals and choropleth listeners.</done>
</task>

<task type="auto">
  <name>Task 2: Create ocean_ph.js and ocean_plastic.js topic modules (OCEAN-02, OCEAN-03)</name>
  <files>detail/topics/ocean_ph.js, detail/topics/ocean_plastic.js</files>
  <action>
**A. Create detail/topics/ocean_ph.js (OCEAN-02):**

Header: World.One 1.0 -- Ocean pH Topic Module (OCEAN-02)

Imports: i18n, DOMUtils, fetchTopicData (for fallback only), createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba

meta: id: 'ocean_ph', titleKey: 'detail.ocean_ph.title', category: 'ocean', icon: ''

Module state: _intervals = [], _cacheData = null

**Hardcoded data (no ocean pH cache exists):**
- PH_TREND: Array of ~35 data points from 1850 to 2025. Key values: 1850: 8.25, 1900: 8.22, 1950: 8.18, 1970: 8.16, 1980: 8.14, 1990: 8.12, 2000: 8.10, 2010: 8.09, 2020: 8.08, 2025: 8.08. Interpolate reasonable values between decades. Source: NOAA PMEL.
- PH_SCALE_ITEMS: Array of pH scale markers from pH 0 to 14 with labels: 0 Battery Acid, 1 Stomach Acid, 3 Vinegar, 5.5 Rain, 7 Pure Water, 8.08 Current Ocean, 8.25 Pre-industrial Ocean, 10 Soap, 14 Drain Cleaner
- ACIDIFICATION_IMPACTS: Array of marine life impacts: Coral (shells dissolve below 7.8), Oysters (larval failure below 7.95), Pteropods (shell thinning at current pH), Clownfish (behavior changes at 7.8)

render(blocks):
1. **Hero:** Display current pH value "8.08" with change from pre-industrial (8.25 - 8.08 = -0.17). Color in ocean blue (#5ac8fa). Tier badge as 'static' since no live pH API. Explain the 26% acidity increase per 0.1 pH drop (logarithmic scale).

2. **Chart:** pH trend line chart (1850-2025) using PH_TREND data. Y-axis inverted feel (higher pH at top but values decrease over time showing acidification). Single descending line in blue with fill. Annotation at pH 8.25 (pre-industrial) as dashed reference line.

3. **Trend block:** pH scale visualization. Create a vertical or horizontal color gradient bar (pH 0-14) using DOM elements. Position markers for "Current Ocean" and "Pre-industrial Ocean" on the scale. Highlight the ocean zone. Use flexbox + absolute positioning, not Chart.js.

4. **Tiles:** Acidification impact cards. 4 cards showing marine organisms affected: coral, oysters, pteropods, clownfish. Each card has organism name, pH threshold, and impact description. Built with DOMUtils.create().

5. **Explanation:** Text about ocean acidification mechanism (CO2 + H2O -> H2CO3 -> H+ + HCO3-), why 0.1 pH change is significant (logarithmic), what it means for marine life.

6. **Comparison:** "Then vs Now" comparison showing pre-industrial ocean conditions vs current with visual indicators.

7. **Sources:** NOAA PMEL, IPCC AR6, Doney et al. 2009

getChartConfigs(): Return empty array.
cleanup(): Clear _intervals, null _cacheData.

---

**B. Create detail/topics/ocean_plastic.js (OCEAN-03):**

Header: World.One 1.0 -- Ocean Plastic Topic Module (OCEAN-03)

Imports: i18n, DOMUtils, fetchTopicData (for fallback only), createTierBadge

meta: id: 'ocean_plastic', titleKey: 'detail.ocean_plastic.title', category: 'ocean', icon: ''

Module state: _intervals = [], _cacheData = null

**Hardcoded data (no plastic cache exists):**
- PLASTIC_STATS: { total_mt: 170, annual_input_mt: 11, daily_input_kg: 23_000_000 } (GESAMP/UNEP 2023)
- GARBAGE_PATCHES: Array of 5 patches with name, center lat/lon, estimated size in km2:
  - North Pacific: { name: 'North Pacific Gyre', cx: 340, cy: 170, size: 1600000 }
  - South Pacific: { name: 'South Pacific Gyre', cx: 280, cy: 310, size: 2600000 }
  - North Atlantic: { name: 'North Atlantic Gyre', cx: 550, cy: 180, size: 1100000 }
  - South Atlantic: { name: 'South Atlantic Gyre', cx: 530, cy: 330, size: 700000 }
  - Indian Ocean: { name: 'Indian Ocean Gyre', cx: 700, cy: 300, size: 900000 }
  (cx/cy are SVG coordinates in a 1000x500 viewBox)
- DECOMPOSITION_TIMES: Array of common items: [{ item: 'Plastic bag', years: 20 }, { item: 'Styrofoam cup', years: 50 }, { item: 'Aluminum can', years: 200 }, { item: 'Plastic bottle', years: 450 }, { item: 'Fishing line', years: 600 }]

render(blocks):
1. **Hero:** Display "~170 Mio t" total ocean plastic. Tier badge as 'static'. Show annual input rate (11 Mt/year). Color in warning orange.

2. **Chart block:** Animated daily counter showing kg of plastic entering the ocean TODAY. Use the population.js setInterval pattern:
   - Calculate startOfDay epoch
   - dailyRate = 23_000_000 kg
   - Update every second: Math.floor(dailyRate * elapsed).toLocaleString() + " kg"
   - Push intervalId to _intervals array
   - Display as large animated number with "heute / today" label below

3. **Trend block:** 5 Garbage Patches SVG map. Create SVG (viewBox 0 0 1000 500):
   - Draw simplified continent outlines (reuse the pattern from earthquakes.js/conflicts.js)
   - Place 5 translucent pulsing circles at GARBAGE_PATCHES coordinates
   - Circle radius proportional to patch size (log scale)
   - Each circle: fill with semi-transparent orange/red, add CSS pulse animation
   - Tooltip on hover showing patch name + estimated size
   Use document.createElementNS for all SVG elements, not innerHTML.

4. **Tiles block:** Decomposition time visualization. For each item in DECOMPOSITION_TIMES, create a horizontal bar with:
   - Item name on the left
   - Proportional bar width (log scale: 20yr -> short, 600yr -> full width)
   - Years label on the right
   - Color gradient from yellow (short) to red (long)
   Built entirely with DOMUtils.create() and inline styles.

5. **Explanation:** Text about microplastics, how plastic enters the ocean (rivers, coastal, fishing), why it matters for marine food chains.

6. **Comparison:** Stats comparison: "Every minute" counter (how much plastic enters ocean per minute = ~16,000 kg), "Every year" (11M tonnes), "Total accumulated" (170M tonnes).

7. **Sources:** GESAMP, UNEP 2023, Jambeck et al. 2015, NOAA Marine Debris Program

getChartConfigs(): Return empty array (no Chart.js used -- all DOM/SVG).
cleanup(): Clear all _intervals (counter), null _cacheData. Critical: counter setInterval MUST be in _intervals.
  </action>
  <verify>
    <automated>node -e "const ph=require('fs').readFileSync('detail/topics/ocean_ph.js','utf8'); const pl=require('fs').readFileSync('detail/topics/ocean_plastic.js','utf8'); ['export const meta','export async function render','export function getChartConfigs','export function cleanup'].forEach(s=>{console.assert(ph.includes(s),'ocean_ph missing: '+s); console.assert(pl.includes(s),'ocean_plastic missing: '+s)}); console.assert(ph.includes('PH_TREND'),'missing pH trend data'); console.assert(pl.includes('GARBAGE_PATCHES'),'missing garbage patches'); console.assert(pl.includes('_intervals.push'),'missing interval tracking in plastic'); console.assert(pl.includes('DECOMPOSITION'),'missing decomposition data'); console.log('ocean_ph.js + ocean_plastic.js OK')"</automated>
  </verify>
  <done>detail/?topic=ocean_ph shows pH hero, descending pH trend chart, interactive pH scale, acidification impact cards, and sources. detail/?topic=ocean_plastic shows plastic estimate hero, live daily counter ticking every second, 5 garbage patches SVG map with pulsing markers, decomposition time bars, and sources. Both modules properly track intervals for cleanup.</done>
</task>

</tasks>

<verification>
1. Navigate to detail/?topic=ocean_temp -- hero shows SST anomaly from ocean.json cache, SST timeline chart renders 1880-present, coral bleaching threshold infographic visible, regional choropleth map renders with colored countries
2. Navigate to detail/?topic=ocean_ph -- hero shows pH 8.08 with -0.17 change, pH trend chart shows declining line 1850-2025, pH scale visualization positions ocean marker correctly, 4 marine impact cards visible
3. Navigate to detail/?topic=ocean_plastic -- hero shows ~170 Mio t, daily counter ticks every second showing kg added today, SVG map shows 5 pulsing garbage patch circles, decomposition bars show 5 items with proportional widths
4. Navigate away from each topic and back -- no console errors, counters restart correctly (cleanup working)
5. Toggle language on each page -- i18n keys translate correctly
</verification>

<success_criteria>
- All 3 ocean topic modules export meta, render, getChartConfigs, cleanup
- ocean_temp.js fetches from ocean.json cache and renders SST timeline + choropleth
- ocean_ph.js uses hardcoded pH trend data and renders declining pH chart + scale visualization
- ocean_plastic.js has animated daily counter with proper interval cleanup + SVG garbage patches map
- All modules use DOMUtils.create() (no innerHTML for data content)
- All setInterval calls tracked in _intervals array for cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/10-new-acts-bio-ocean-topics/10-02-SUMMARY.md`
</output>
