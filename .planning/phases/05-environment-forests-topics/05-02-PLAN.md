---
phase: 05-environment-forests-topics
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - detail/topics/airquality.js
  - detail/topics/forests.js
autonomous: true
requirements: [ENV-04, ENV-05]

must_haves:
  truths:
    - "User visits detail/?topic=airquality and sees global AQI value in hero"
    - "User sees city AQI rankings with cleanest and most polluted cities"
    - "User sees AQI-vs-GDP scatter plot showing relationship between wealth and air quality"
    - "User sees PM2.5/PM10/NO2 pollutant explainer cards with WHO thresholds"
    - "User visits detail/?topic=forests and sees forest cover percentage in hero"
    - "User sees annual forest loss chart from 2000 to present"
    - "User sees deforestation causes as stacked bar chart with 5 categories"
  artifacts:
    - path: "detail/topics/airquality.js"
      provides: "Air quality topic module with AQI hero, city rankings, scatter, explainer"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
    - path: "detail/topics/forests.js"
      provides: "Forests topic module with cover %, annual loss, deforestation causes"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
  key_links:
    - from: "detail/topics/airquality.js"
      to: "js/utils/data-loader.js"
      via: "fetchTopicData and fetchWithTimeout for Open-Meteo Air Quality API"
      pattern: "fetchTopicData|fetchWithTimeout"
    - from: "detail/topics/airquality.js"
      to: "js/utils/chart-manager.js"
      via: "ensureChartJs + createChart for scatter plot"
      pattern: "createChart.*scatter"
    - from: "detail/topics/forests.js"
      to: "js/utils/chart-manager.js"
      via: "ensureChartJs + createChart for line and stacked bar"
      pattern: "createChart"
    - from: "detail/topics/forests.js"
      to: "js/utils/data-loader.js"
      via: "fetchTopicData('forests')"
      pattern: "fetchTopicData.*forests"
---

<objective>
Implement the air quality topic module (ENV-04) and forests topic module (ENV-05) as detail page topics.

Purpose: Air quality adds the first scatter chart type to the detail page system and demonstrates live Open-Meteo API integration. Forests completes the simplest environment topic with straightforward World Bank data visualization.

Output: detail/topics/airquality.js, detail/topics/forests.js
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-environment-forests-topics/05-RESEARCH.md
@.planning/phases/05-environment-forests-topics/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and existing infrastructure -->

From detail/utils/choropleth.js (created in Plan 01):
```javascript
export async function renderChoropleth(container, {
  dataMap,      // ISO-2 -> numeric value
  colorFn,      // (value) => CSS color
  tooltipFn,    // (iso, value) => tooltip text
  legendItems,  // [{ color, label }]
  title,        // optional heading
  defaultColor  // optional, default 'rgba(255,255,255,0.05)'
});
// Returns { wrapper, cleanup }
```

From js/utils/data-loader.js:
```javascript
export async function fetchWithTimeout(url, timeoutMs = 5000);
export async function fetchTopicData(topic, apiUrl = null);
// Returns { data, tier, age }
```

From js/utils/chart-manager.js:
```javascript
export function ensureChartJs();
export const CHART_COLORS = { environment: { r: 0, g: 180, b: 216 } };
export function toRgba({ r, g, b }, alpha = 1);
export function createChart(canvasId, config);
```

From js/utils/dom.js:
```javascript
export const DOMUtils = { create(tag, props, children), $(selector, parent) };
```

From js/utils/badge.js:
```javascript
export function createTierBadge(tier, options);
```

Topic module contract (all modules export):
```javascript
export const meta = { id, titleKey, category, icon };
export async function render(blocks);        // blocks.hero, .chart, .trend, .tiles, .explanation, .comparison, .sources
export function getChartConfigs();            // [{ canvasId, blockId, config }] for lazy init
export function cleanup();
```

i18n keys available (added in Plan 01):
- detail.airquality.* (title, heroLabel, heroUnit, citiesTitle, scatterTitle, explainerTitle, tiles, explanation, comparison, sources)
- detail.forests.* (title, heroLabel, heroUnit, lossTitle, causesTitle, tiles, explanation, comparison, sources)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement air quality topic module (ENV-04)</name>
  <files>detail/topics/airquality.js</files>
  <action>
Create detail/topics/airquality.js following the topic module contract.

**Imports:** Same as biodiversity.js pattern (i18n, DOMUtils, MathUtils, fetchTopicData, fetchWithTimeout, createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba). No choropleth import needed (air quality uses scatter chart instead of map).

**meta:** id 'airquality', titleKey 'detail.airquality.title', category 'environment', icon ''

**Module state:** `let _chartData = null;`

**render(blocks):**

1. **Data fetch:**
   - Try live AQI from Open-Meteo Air Quality API (same API used in co2.js for ppm):
     `https://air-quality-api.open-meteo.com/v1/air-quality?latitude=52.52&longitude=13.41&current=european_aqi`
     Use fetchWithTimeout(url, 5000), extract current.european_aqi
   - Fallback to fetchTopicData('airquality') which returns cache/static
   - Store tier and age from whichever source succeeded

2. **Hero block (blocks.hero):**
   - Value: current AQI number
   - Unit: i18n.t('detail.airquality.heroUnit') -> "AQI"
   - Label: i18n.t('detail.airquality.heroLabel')
   - Badge: createTierBadge(tier, { age })
   - AQI color indicator: add colored dot next to value (0-50: #4caf50 good, 51-100: #ffc107 moderate, 101-150: #ff9800 unhealthy-sensitive, 151+: #f44336 unhealthy)

3. **Primary chart block (blocks.chart) -- City AQI Rankings:**
   - Heading: i18n.t('detail.airquality.citiesTitle')
   - Hardcode 20 cities with AQI values (10 cleanest + 10 most polluted, from Open-Meteo/WHO 2024 data):
     Cleanest: Helsinki 18, Stockholm 22, Zurich 25, Auckland 26, Vancouver 28, Reykjavik 20, Hobart 24, Edinburgh 27, Oslo 23, Copenhagen 26
     Most polluted: Delhi 285, Dhaka 248, Lahore 230, Kolkata 195, Beijing 160, Jakarta 145, Cairo 140, Mumbai 135, Ho Chi Minh 120, Baghdad 115
   - Render as horizontal bar chart using Chart.js:
     - type: 'bar'
     - indexAxis: 'y' (horizontal bars)
     - Cleanest cities: green (#4caf50), Polluted cities: red (#f44336)
     - Canvas id: 'airquality-cities-chart', container height: 450px
   - Render directly in render() (interactive, responds to sorting)
   - Store config in _chartData for reference

4. **Trend block (blocks.trend) -- AQI vs GDP Scatter Plot:**
   - Heading: i18n.t('detail.airquality.scatterTitle')
   - Hardcode 25 countries with AQI and GDP per capita (from World Bank 2023 + Open-Meteo):
     ```
     { iso: 'US', aqi: 45, gdp: 76330, label: 'USA' },
     { iso: 'DE', aqi: 38, gdp: 48718, label: 'Germany' },
     { iso: 'IN', aqi: 170, gdp: 2389, label: 'India' },
     { iso: 'CN', aqi: 110, gdp: 12720, label: 'China' },
     { iso: 'JP', aqi: 42, gdp: 33815, label: 'Japan' },
     { iso: 'BR', aqi: 55, gdp: 8917, label: 'Brazil' },
     { iso: 'NG', aqi: 130, gdp: 1621, label: 'Nigeria' },
     { iso: 'GB', aqi: 35, gdp: 46371, label: 'UK' },
     { iso: 'FR', aqi: 40, gdp: 42330, label: 'France' },
     { iso: 'BD', aqi: 200, gdp: 2688, label: 'Bangladesh' },
     { iso: 'PK', aqi: 180, gdp: 1505, label: 'Pakistan' },
     { iso: 'ID', aqi: 95, gdp: 4788, label: 'Indonesia' },
     { iso: 'EG', aqi: 120, gdp: 3699, label: 'Egypt' },
     { iso: 'ZA', aqi: 60, gdp: 6001, label: 'S. Africa' },
     { iso: 'AU', aqi: 30, gdp: 65099, label: 'Australia' },
     { iso: 'CA', aqi: 32, gdp: 52722, label: 'Canada' },
     { iso: 'KR', aqi: 65, gdp: 32423, label: 'S. Korea' },
     { iso: 'SE', aqi: 22, gdp: 55873, label: 'Sweden' },
     { iso: 'NO', aqi: 20, gdp: 82832, label: 'Norway' },
     { iso: 'PL', aqi: 55, gdp: 18000, label: 'Poland' },
     { iso: 'MX', aqi: 75, gdp: 10948, label: 'Mexico' },
     { iso: 'SA', aqi: 95, gdp: 27044, label: 'Saudi Arabia' },
     { iso: 'TR', aqi: 70, gdp: 10674, label: 'Turkiye' },
     { iso: 'TH', aqi: 80, gdp: 7066, label: 'Thailand' },
     { iso: 'ET', aqi: 85, gdp: 1027, label: 'Ethiopia' }
     ```
   - Chart.js scatter chart:
     - type: 'scatter'
     - x-axis: GDP per capita (logarithmic scale)
     - y-axis: AQI (lower is better)
     - Point color: mapped by AQI (green < 50, yellow 50-100, orange 100-150, red > 150)
     - pointRadius: 6
     - Tooltip: shows country label, AQI, GDP
     - Canvas id: 'airquality-scatter-chart', container height: 350px
   - Render directly in render() with ensureChartJs() + createChart()

5. **Tiles block (blocks.tiles) -- 2x2 WHO guideline tiles:**
   - PM2.5: i18n.t('detail.airquality.tilePM25') -> "15 ug/m3" (WHO 2021 annual limit)
   - PM10: i18n.t('detail.airquality.tilePM10') -> "45 ug/m3"
   - NO2: i18n.t('detail.airquality.tileNO2') -> "25 ug/m3"  (WHO 2021)
   - Deaths: i18n.t('detail.airquality.tileDeaths') -> "4.2M"
   - Standard 2x2 grid from co2.js pattern

6. **Explanation block (blocks.explanation):**
   - Heading: i18n.t('detail.airquality.explainerTitle')
   - Pollutant explainer cards: 3 cards for PM2.5, PM10, NO2
   - Each card: pollutant name, WHO threshold, health effects, main sources
   - Use DOMUtils.create for card layout (flexbox column, glass-morphism background)
   - PM2.5: Threshold 15 ug/m3 annual, causes respiratory/cardiovascular disease, from combustion/industry
   - PM10: Threshold 45 ug/m3 annual, causes lung inflammation, from dust/construction/traffic
   - NO2: Threshold 25 ug/m3 annual, causes airway inflammation, from vehicles/power plants
   - Below cards: i18n.t('detail.airquality.explanation') paragraph

7. **Comparison block (blocks.comparison):**
   - i18n.t('detail.airquality.comparison') text

8. **Sources block (blocks.sources):**
   - Open-Meteo: https://open-meteo.com/en/docs/air-quality-api
   - WHO: https://www.who.int/news-room/fact-sheets/detail/ambient-(outdoor)-air-quality-and-health

**getChartConfigs():** Return empty array (both charts rendered directly in render for interactivity).

**cleanup():** Null out _chartData.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const f = fs.readFileSync('detail/topics/airquality.js','utf8'); const checks = ['export const meta', 'export async function render', 'export function getChartConfigs', 'export function cleanup', 'fetchWithTimeout', 'scatter', 'CHART_COLORS']; const pass = checks.every(c => f.includes(c)); console.log('Airquality module:', pass ? 'PASS' : 'FAIL - missing: ' + checks.filter(c => !f.includes(c)).join(', '))"</automated>
  </verify>
  <done>detail/?topic=airquality renders hero with live AQI + badge, horizontal bar chart of 20 cities (10 cleanest + 10 most polluted), AQI-vs-GDP scatter plot with 25 countries, 2x2 WHO guideline tiles, pollutant explainer cards (PM2.5/PM10/NO2), and 2 source links.</done>
</task>

<task type="auto">
  <name>Task 2: Implement forests topic module (ENV-05)</name>
  <files>detail/topics/forests.js</files>
  <action>
Create detail/topics/forests.js following the topic module contract. This is the simplest topic with 3 main sections and no choropleth.

**Imports:** i18n, DOMUtils, fetchTopicData, createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba. No choropleth, no MathUtils needed.

**meta:** id 'forests', titleKey 'detail.forests.title', category 'environment', icon ''

**Module state:** `let _chartData = null;`

**render(blocks):**

1. **Data fetch:** `const { data, tier, age } = await fetchTopicData('forests');`
   - Primary data: forest cover percentage
   - If data has value, use it. Otherwise fallback to static 31.2%

2. **Hero block (blocks.hero):**
   - Value: forest cover % (e.g., "31.2")
   - Unit: "%" from i18n
   - Label: i18n.t('detail.forests.heroLabel')
   - Badge: createTierBadge(tier, { age })
   - Standard hero pattern

3. **Primary chart block (blocks.chart) -- Annual Forest Loss Since 2000:**
   - Heading: i18n.t('detail.forests.lossTitle')
   - Hardcode FOREST_LOSS data (annual net loss in million hectares, from World Bank/FAO):
     ```
     2001: -7.8, 2002: -7.5, 2003: -7.2, 2004: -7.6, 2005: -7.3,
     2006: -6.8, 2007: -6.5, 2008: -6.1, 2009: -5.8, 2010: -5.2,
     2011: -5.5, 2012: -5.8, 2013: -6.0, 2014: -5.5, 2015: -5.0,
     2016: -5.2, 2017: -4.8, 2018: -4.7, 2019: -4.7, 2020: -4.7,
     2021: -4.1, 2022: -3.7
     ```
     (Negative values = net loss. Trend shows gradual improvement due to reforestation offsetting some deforestation.)
   - Chart.js bar chart:
     - type: 'bar'
     - Bars colored red (loss): toRgba({ r: 211, g: 47, b: 47 }, 0.8)
     - Canvas id: 'forests-loss-chart', container height: 350px
     - Y-axis label: "Million hectares (net loss)"
   - Render directly in render() for timerangechange interactivity
   - Store chart data in _chartData

4. **Trend block (blocks.trend) -- Deforestation Causes (stacked bar):**
   - Heading: i18n.t('detail.forests.causesTitle')
   - Hardcode DEFORESTATION_CAUSES from FAO Global Forest Resources Assessment:
     ```
     Agriculture expansion: 73% (color: #e65100)
     Logging (commercial): 10% (color: #795548)
     Wildfires: 8% (color: #f44336)
     Urbanization: 5% (color: #9e9e9e)
     Infrastructure (roads, mining): 4% (color: #607d8b)
     ```
   - Render as single stacked horizontal bar showing proportions:
     - type: 'bar'
     - indexAxis: 'y'
     - stacked: true on both x and y scales
     - 5 datasets (one per cause), each with single data value (%)
     - Canvas id: 'forests-causes-chart', container height: 120px
   - This chart is supplementary -- render via getChartConfigs() for lazy loading

5. **Tiles block (blocks.tiles) -- 2x2:**
   - Annual loss: i18n.t('detail.forests.tileLoss') -> "-3.7M ha" (2022 figure)
   - Forest area: i18n.t('detail.forests.tileArea') -> "4.06B ha"
   - Primary forest: i18n.t('detail.forests.tilePrimary') -> "34%" (of total forest)
   - Loss rate: i18n.t('detail.forests.tileRate') -> "-0.09%/yr"

6. **Explanation block (blocks.explanation):**
   - i18n.t('detail.forests.explanation') paragraph

7. **Comparison block (blocks.comparison):**
   - i18n.t('detail.forests.comparison') text
   - Additionally: Add a simple top-5 country forest area ranking using DOMUtils (not a chart):
     1. Russia: 815M ha
     2. Brazil: 497M ha
     3. Canada: 347M ha
     4. USA: 310M ha
     5. China: 220M ha
   - Styled as a numbered list with bar widths proportional to area

8. **Sources block (blocks.sources):**
   - World Bank: https://data.worldbank.org/indicator/AG.LND.FRST.ZS
   - FAO: https://www.fao.org/forest-resources-assessment

**getChartConfigs():** Return the deforestation causes stacked bar chart config:
```javascript
[{
  canvasId: 'forests-causes-chart',
  blockId: 'trend',
  config: { /* stacked bar config */ }
}]
```

**cleanup():** Null out _chartData.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const f = fs.readFileSync('detail/topics/forests.js','utf8'); const checks = ['export const meta', 'export async function render', 'export function getChartConfigs', 'export function cleanup', 'FOREST_LOSS', 'DEFORESTATION_CAUSES', 'stacked']; const pass = checks.every(c => f.includes(c)); console.log('Forests module:', pass ? 'PASS' : 'FAIL - missing: ' + checks.filter(c => !f.includes(c)).join(', '))"</automated>
  </verify>
  <done>detail/?topic=forests renders hero with forest cover % + badge, annual loss bar chart (2001-2022), deforestation causes stacked bar (5 categories), 2x2 context tiles, explanation, top-5 country ranking, and 2 source links. getChartConfigs returns the causes chart for lazy loading.</done>
</task>

</tasks>

<verification>
- detail/topics/airquality.js exports meta, render, getChartConfigs, cleanup
- detail/topics/forests.js exports meta, render, getChartConfigs, cleanup
- Air quality module fetches live AQI from Open-Meteo and renders scatter + bar charts
- Forests module renders annual loss bar chart and deforestation causes stacked bar
- Both modules use standard imports and topic contract pattern
</verification>

<success_criteria>
1. detail/?topic=airquality shows AQI hero, city rankings, AQI-vs-GDP scatter, WHO tiles, pollutant explainer
2. detail/?topic=forests shows forest cover hero, annual loss chart, deforestation causes stacked bar
3. Both modules handle data fallbacks gracefully (tier badge shows correct state)
4. No console errors from either module
</success_criteria>

<output>
After completion, create `.planning/phases/05-environment-forests-topics/05-02-SUMMARY.md`
</output>
