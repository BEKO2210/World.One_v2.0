---
phase: 07-economy-progress-weather
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - detail/topics/science.js
  - detail/topics/internet.js
autonomous: true
requirements: [PROG-01, PROG-02]

must_haves:
  truths:
    - "User visits detail/?topic=science and sees total arXiv papers count in hero"
    - "Exponential growth curve shows annual arXiv submissions 1991-2025"
    - "Hot research fields bar chart displays category distribution from cache"
    - "Nobel prizes bubble chart shows top 15 countries by prize count"
    - "User visits detail/?topic=internet and sees internet users percentage hero"
    - "Real-time counters for emails, Google searches, and YouTube uploads tick up every second"
    - "Digital divide SVG choropleth colors countries by internet penetration"
  artifacts:
    - path: "detail/topics/science.js"
      provides: "Science topic module with growth curve, fields bar, Nobel bubble"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
    - path: "detail/topics/internet.js"
      provides: "Internet topic module with counters, choropleth"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
  key_links:
    - from: "detail/topics/science.js"
      to: "js/utils/data-loader.js"
      via: "fetchTopicData('science') for arXiv cache"
      pattern: "fetchTopicData\\('science'\\)"
    - from: "detail/topics/science.js"
      to: "js/utils/chart-manager.js"
      via: "createChart for growth curve, bar, and bubble charts"
      pattern: "createChart\\("
    - from: "detail/topics/internet.js"
      to: "detail/utils/choropleth.js"
      via: "renderChoropleth for digital divide map"
      pattern: "renderChoropleth\\("
    - from: "detail/topics/internet.js"
      to: "js/utils/math.js"
      via: "MathUtils.formatCompact for counter display"
      pattern: "formatCompact\\("
---

<objective>
Implement science and internet topic modules. Both use hardcoded/calculated data with no live API polling, making them naturally parallel-safe and simpler than the live-data topics in Plan 03.

Purpose: Complete the progress topic category (PROG-01 science, PROG-02 internet). Science proves the arXiv cache integration and Chart.js bubble chart pattern. Internet proves the calculated real-time counter pattern (like population.js births/deaths) and reuses the choropleth component.

Output: Two working detail pages at detail/?topic=science and detail/?topic=internet.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-economy-progress-weather/07-RESEARCH.md

<interfaces>
<!-- All standard topic module imports (same as Plan 01) -->

From js/utils/data-loader.js:
```javascript
export async function fetchTopicData(topicId); // Returns { data, tier, age }
export async function fetchWithTimeout(url, timeoutMs);
```

From js/utils/chart-manager.js:
```javascript
export async function ensureChartJs();
export function createChart(canvasEl, config);
export const CHART_COLORS; // { environment, society, economy, realtime, progress }
export function toRgba(colorObj, alpha);
```

From detail/utils/choropleth.js:
```javascript
export function renderChoropleth(container, { dataMap, colorFn, tooltipFn, legendItems });
// Returns { cleanup: Function } for teardown
```

From data/cache/arxiv-ai.json (existing cache):
```json
{
  "_meta": { "fetched_at": "...", "source": "arXiv", "version": "1.0" },
  "papers": [
    { "title": "...", "published": "2026-03-20", "summary": "...", "url": "...", "primary_category": "cs.AI" },
    ...
  ]
}
```

<!-- population.js setInterval/cleanup pattern (for internet counters) -->
From detail/topics/population.js:
```javascript
let _intervals = [];
// In render():
_intervals.push(setInterval(updateFn, 1000));
// In cleanup():
export function cleanup() {
  _intervals.forEach(id => clearInterval(id));
  _intervals = [];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement science topic module</name>
  <files>detail/topics/science.js</files>
  <action>
Create detail/topics/science.js implementing the full topic contract. Follow the poverty.js module structure exactly.

**Imports:** i18n, DOMUtils, MathUtils, fetchTopicData, createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba

**Meta:** { id: 'science', titleKey: 'detail.science.title', category: 'progress', icon: '' }

**Module state:** _chartData = null for lazy charts, _growthChart = null for the directly rendered growth chart

**Hero block (block 0 -- primary-chart):**
- Fetch topic data via fetchTopicData('science') -- pulls arxiv-ai.json cache
- Display total arXiv papers: ~2,990,000 (hardcoded, arXiv stats 2026)
- Show daily submissions: ~1,800 (hardcoded)
- createTierBadge with data tier

**Exponential growth curve (block 1 -- trend, rendered directly for timerangechange):**
- Hardcode annual arXiv submission counts from 1991 to 2025 (35 data points). Approximate values:
  1991: 3000, 1995: 20000, 2000: 40000, 2005: 55000, 2010: 75000, 2012: 85000, 2014: 100000, 2016: 120000, 2018: 150000, 2019: 165000, 2020: 190000, 2021: 210000, 2022: 230000, 2023: 250000, 2024: 270000, 2025: 290000
  (Fill intermediate years with linear interpolation between these anchors)
- Render as Chart.js line chart directly in render() (not via getChartConfigs) so it can respond to timerangechange events
- Listen for 'timerangechange' CustomEvent on the trend block to filter data by range (1Y, 5Y, 20Y, Max)
- Use CHART_COLORS.progress for line color
- Title from i18n: detail.science.growthTitle

**Hot research fields bar chart (stored for getChartConfigs):**
- Extract primary_category from cached papers array
- Map granular arXiv categories to broad fields: cs.AI/cs.LG/cs.CL/cs.CV -> "AI & ML", physics.* -> "Physics", math.* -> "Mathematics", stat.* -> "Statistics", q-bio.* -> "Biology", cond-mat.* -> "Materials", astro-ph.* -> "Astrophysics", econ.* -> "Economics", other -> "Other"
- If cache has < 5 papers with categories, fall back to hardcoded field distribution: { "AI & ML": 45, "Physics": 20, "Mathematics": 15, "Statistics": 8, "Biology": 5, "Other": 7 }
- Horizontal bar chart sorted by count descending
- Use CHART_COLORS.progress with varying alpha for each bar

**Nobel prizes bubble chart (stored for getChartConfigs):**
- Hardcode top 15 countries by Nobel Prize count (data changes yearly, not worth live API call):
  USA: 400, UK: 137, Germany: 112, France: 72, Sweden: 32, Switzerland: 28, Japan: 28, Russia: 27, Canada: 26, Austria: 22, Netherlands: 22, Italy: 20, Denmark: 14, Norway: 13, Australia: 12
- Chart.js bubble chart: x = index, y = count, r = sqrt(count) scaled
- Per-point colors from a palette array
- Title from i18n: detail.science.nobelTitle

**Context tiles (block 3):**
- Tile 1: Daily new papers (~1,800)
- Tile 2: Growth rate ("97x since 1991")
- Tile 3: Research fields tracked

**Explanation, Comparison, Sources (blocks 4-6):** Text from i18n keys. Source: arXiv.org, Nobel Prize API

**cleanup():** _chartData = null, _growthChart = null (no intervals)
  </action>
  <verify>
    <automated>node -e "import('./detail/topics/science.js').then(m => { const ok = m.meta && m.meta.id === 'science' && m.meta.category === 'progress' && typeof m.render === 'function' && typeof m.getChartConfigs === 'function' && typeof m.cleanup === 'function'; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>detail/?topic=science renders: total papers hero, exponential growth line chart (responds to time range), research fields horizontal bar, Nobel bubble chart, tiles, explanation, sources. No console errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement internet topic module</name>
  <files>detail/topics/internet.js</files>
  <action>
Create detail/topics/internet.js implementing the full topic contract. Uses the population.js setInterval pattern for real-time counters and the choropleth.js component for the digital divide map.

**Imports:** i18n, DOMUtils, MathUtils, fetchTopicData (for static fallback only), createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba, renderChoropleth from '../../detail/utils/choropleth.js'

**Meta:** { id: 'internet', titleKey: 'detail.internet.title', category: 'progress', icon: '' }

**Module state:** let _intervals = [], _choroplethCleanup = null, _chartData = null

**Hero block (block 0 -- primary-chart):**
- Internet users: 67% global (~5.4 billion) -- hardcoded World Bank IT.NET.USER.ZS 2024
- Show as large percentage with context text
- createTierBadge with 'static' tier (no live API for this metric)

**Real-time counters (block 1 -- trend, rendered directly in render()):**
- Three counters that calculate from known daily rates, same pattern as population.js births/deaths clock:
  - Emails sent today: 332,000,000,000 per day (Statista 2025)
  - Google searches today: 8,500,000,000 per day (Internet Live Stats)
  - YouTube hours uploaded today: 720,000 hours per day (500 hours/min * 60 * 24)
- For each counter:
  - Calculate perSecond = dailyRate / 86400
  - startOfDay = new Date() with hours/min/sec/ms set to 0
  - update function: elapsed = (Date.now() - startOfDay) / 1000; value = Math.round(elapsed * perSecond)
  - Display with MathUtils.formatCompact() for readable numbers
- Store interval IDs: _intervals.push(setInterval(update, 1000))
- Build counter cards with DOMUtils.create: icon/emoji, label from i18n, value element with monospace styling

**Digital divide choropleth (block 3 -- tiles area, rendered directly):**
- Hardcode internet penetration data for ~40 countries (World Bank 2024 estimates):
  Top tier (>90%): DK 98, IS 99, AE 99, KR 98, GB 96, SE 96, NL 95, DE 93, JP 93, US 92, CA 93, AU 92
  High (70-90%): FR 88, ES 87, IT 84, BR 81, RU 88, TR 83, AR 87, SA 82
  Medium (40-70%): CN 73, MX 72, ZA 72, CO 69, EG 55, ID 62, PH 53, IN 52, NG 55, KE 42, VN 70
  Low (<40%): BD 38, PK 33, ET 17, CD 23, TZ 25, MM 44, MZ 18, MG 15, TD 14, NE 11
- Use renderChoropleth from detail/utils/choropleth.js with:
  - dataMap: ISO-2 code -> percentage
  - colorFn: gradient from red (<20%) through yellow (50%) to green (>80%)
  - tooltipFn: "Country: XX% internet penetration"
  - legendItems: [">90% Connected", "70-90%", "40-70%", "<40%"]
- Store cleanup: _choroplethCleanup = renderChoropleth(...).cleanup

**Context tiles (block 4 -- explanation area above text):**
- Tile 1: Total internet users (5.4B)
- Tile 2: Mobile internet share (~60%)
- Tile 3: Annual growth rate (~3%)

**Explanation, Comparison, Sources (blocks 5-6):** Text from i18n keys. Source: World Bank, ITU, Statista

**cleanup():** Clear all intervals (_intervals.forEach(clearInterval), _intervals = []), call _choroplethCleanup?.(), _chartData = null
  </action>
  <verify>
    <automated>node -e "import('./detail/topics/internet.js').then(m => { const ok = m.meta && m.meta.id === 'internet' && m.meta.category === 'progress' && typeof m.render === 'function' && typeof m.cleanup === 'function'; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>detail/?topic=internet renders: 67% internet users hero, three real-time counters ticking every second (emails, Google, YouTube), digital divide choropleth with colored countries and legend, tiles, explanation, sources. Counters stop on page navigation (cleanup works). No console errors.</done>
</task>

</tasks>

<verification>
- Open detail/?topic=science -- growth curve renders, time range selector works (1Y/5Y/20Y/Max), fields bar and Nobel bubble appear on scroll
- Open detail/?topic=internet -- counters tick up visibly every second, choropleth shows colored countries, switching language translates all text
- Navigate away from internet to another topic -- no "[Internet]" console messages continue (proves cleanup)
</verification>

<success_criteria>
1. Science topic page shows 4 visualizations: growth curve, fields bar, Nobel bubble, tiles
2. Internet topic page shows 3 counters incrementing in real-time plus choropleth map
3. Both modules export meta, render, getChartConfigs, cleanup per contract
4. Internet module properly cleans up intervals and choropleth on navigation
</success_criteria>

<output>
After completion, create `.planning/phases/07-economy-progress-weather/07-02-SUMMARY.md`
</output>
