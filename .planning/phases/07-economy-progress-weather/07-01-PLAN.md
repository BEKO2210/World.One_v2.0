---
phase: 07-economy-progress-weather
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/i18n.js
  - data/fallback/static-values.json
  - detail/topics/currencies.js
autonomous: true
requirements: [ECON-03]

must_haves:
  truths:
    - "All 5 Phase 7 topic i18n keys exist in both DE and EN"
    - "Static fallback values are populated for currencies, science, internet, space, weather"
    - "User visits detail/?topic=currencies and sees exchange rate hero with tier badge"
    - "Currency converter accepts amount input and produces correct conversion using cached rates"
    - "12-month EUR/USD and USD/CNY line charts render in the trend block"
    - "Hyperinflation countries are visually highlighted in a distinct section"
  artifacts:
    - path: "js/i18n.js"
      provides: "DE/EN translation keys for currencies, science, internet, space, weather"
      contains: "detail.currencies."
    - path: "data/fallback/static-values.json"
      provides: "Static fallback values for all 5 Phase 7 topics"
    - path: "detail/topics/currencies.js"
      provides: "Currencies topic module with full contract"
      exports: ["meta", "render", "getChartConfigs", "cleanup"]
  key_links:
    - from: "detail/topics/currencies.js"
      to: "js/i18n.js"
      via: "i18n.t() calls"
      pattern: "i18n\\.t\\('detail\\.currencies\\."
    - from: "detail/topics/currencies.js"
      to: "js/utils/data-loader.js"
      via: "fetchTopicData('currencies')"
      pattern: "fetchTopicData\\('currencies'\\)"
    - from: "detail/topics/currencies.js"
      to: "js/utils/chart-manager.js"
      via: "ensureChartJs + createChart for line charts"
      pattern: "createChart\\("
---

<objective>
Add all Phase 7 i18n translation keys (DE/EN) and static fallback values for 5 topics, then implement the currencies topic module with exchange rate hero, client-side converter, 12-month charts, and hyperinflation highlight.

Purpose: Unblock parallel execution of Plans 02 and 03 by adding all i18n keys upfront (proven pattern from Phases 4-6). Currencies is the simplest topic (uses existing cache, no live API polling) so it pairs naturally with the setup work.

Output: Working currencies detail page at detail/?topic=currencies, plus i18n/fallback infrastructure for all 5 Phase 7 topics.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-economy-progress-weather/07-RESEARCH.md

<interfaces>
<!-- Key imports used by all topic modules (proven in 14 existing modules) -->

From js/i18n.js:
```javascript
export const i18n = { t(key), lang, init() };
```

From js/utils/data-loader.js:
```javascript
export async function fetchTopicData(topicId); // Returns { data, tier, age }
export async function fetchWithTimeout(url, timeoutMs);
```

From js/utils/badge.js:
```javascript
export function createTierBadge(tier, options); // Returns DOM element
```

From js/utils/chart-manager.js:
```javascript
export async function ensureChartJs();
export function createChart(canvasEl, config); // Synchronous, call after ensureChartJs
export const CHART_COLORS; // { environment, society, economy, realtime, progress }
export function toRgba(colorObj, alpha); // Returns 'rgba(r,g,b,a)' string
```

From js/utils/dom.js:
```javascript
export const DOMUtils = { create(tag, attrs, children), $(sel), $$(sel), createSVG(tag, attrs) };
```

From js/utils/math.js:
```javascript
export const MathUtils = { formatNumber(n), formatCompact(n), geoToSVG(lat, lng, w, h) };
```

From data/cache/currencies.json (existing cache structure):
```json
{
  "_meta": { "fetched_at": "...", "source": "Open ER API", "version": "1.0" },
  "base": "USD",
  "rates": { "EUR": 0.92, "GBP": 0.79, "JPY": 149.5, "CNY": 7.24, ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 7 i18n keys and static fallback values</name>
  <files>js/i18n.js, data/fallback/static-values.json</files>
  <action>
1. In js/i18n.js, add DE and EN translation keys for all 5 Phase 7 topics after the existing Phase 6 keys (detail.poverty.* block). Follow the exact naming pattern from Phase 6 topics. Each topic needs approximately:
   - title, heroLabel, heroUnit (hero block)
   - chart/section titles (2-4 per topic)
   - tile labels (3-4 per topic)
   - explanation, comparison (text blocks)
   - source labels (1-2 per topic)
   - Any interactive element labels (converter labels, toggle labels, etc.)

   Topic-specific keys needed:
   - **currencies** (~18 keys): title, heroLabel, heroUnit, converterTitle, converterFrom, converterTo, converterResult, chartEURUSD, chartUSDCNY, tileStrong, tileWeak, tileVolatile, hyperTitle, explanation, comparison, sourceER
   - **science** (~16 keys): title, heroLabel, heroUnit, growthTitle, fieldsTitle, nobelTitle, tileDaily, tileGrowth, tileFields, explanation, comparison, sourceArxiv, sourceNobel
   - **internet** (~16 keys): title, heroLabel, heroUnit, countersTitle, counterEmails, counterGoogle, counterYoutube, mapTitle, tileTotal, tileMobile, tileGrowth, explanation, comparison, sourceWB, clickCountry
   - **space** (~16 keys): title, heroLabel, heroUnit, issTitle, crewTitle, newsTitle, satTitle, tileAltitude, tileSpeed, tileCrew, tileOrbits, explanation, comparison, sourceISS, sourceNews
   - **weather** (~14 keys): title, heroLabel, heroUnit, citiesTitle, warningTitle, tileHot, tileCold, tileWarnings, explanation, comparison, sourceOM, sparklineLabel

2. In data/fallback/static-values.json, replace the null entries for currencies, science, internet, space, and weather with structured fallback data:
   - **currencies**: { "eur_usd": { "value": 0.92, "year": 2025, "source": "Open ER API" }, "usd_cny": { "value": 7.24, "year": 2025, "source": "Open ER API" } }
   - **science**: { "total_papers": { "value": 2990000, "year": 2026, "source": "arXiv" }, "daily_submissions": { "value": 1800, "year": 2025, "source": "arXiv stats" } }
   - **internet**: { "users_pct": { "value": 67, "unit": "%", "year": 2024, "source": "World Bank" }, "total_users_b": { "value": 5.4, "unit": "billion", "year": 2024, "source": "ITU" } }
   - **space**: { "iss_altitude_km": { "value": 408, "year": 2025, "source": "NASA" }, "satellites": { "value": 13000, "year": 2025, "source": "UCS" } }
   - **weather**: { "cities_tracked": { "value": 24, "year": 2025, "source": "Open-Meteo" } }
  </action>
  <verify>
    <automated>node -e "import('./js/i18n.js').then(m => { m.i18n.init(); const keys = ['detail.currencies.title','detail.science.title','detail.internet.title','detail.space.title','detail.weather.title']; const ok = keys.every(k => m.i18n.t(k) !== k); console.log(ok ? 'PASS' : 'FAIL: missing keys'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>All 5 Phase 7 topics have DE/EN i18n keys (no key returns its own key path). Static fallback values are non-null for currencies, science, internet, space, weather.</done>
</task>

<task type="auto">
  <name>Task 2: Implement currencies topic module</name>
  <files>detail/topics/currencies.js</files>
  <action>
Create detail/topics/currencies.js implementing the full topic contract (meta, render, getChartConfigs, cleanup). Follow the poverty.js / inequality.js module structure exactly.

**Module structure:**
1. Imports: i18n, DOMUtils, MathUtils, fetchTopicData, createTierBadge, ensureChartJs, createChart, CHART_COLORS, toRgba
2. Export meta: { id: 'currencies', titleKey: 'detail.currencies.title', category: 'economy', icon: '' }
3. Module state: _chartData for lazy chart configs

**Hero block (block 0 -- primary-chart):**
- Fetch topic data via fetchTopicData('currencies')
- Display key exchange rates from cache: EUR/USD, GBP/USD, JPY/USD, CNY/USD
- Show createTierBadge with data tier
- Format rates with MathUtils.formatNumber

**Currency converter (block 1 -- trend):**
- Build converter UI using DOMUtils.create (no innerHTML)
- Input field: type="number", value="100", min="0", step="any"
- Two select dropdowns populated from cache rate keys (EUR, USD, GBP, JPY, CNY, etc.)
- Result display div
- Conversion logic: all rates are USD-based, so convert(amount, fromRate, toRate) = (amount / fromRate) * toRate
- Event listeners on input/change for instant recalculation
- Guard against NaN: if isNaN(amount) or amount < 0, show "--"

**12-month charts (stored for getChartConfigs):**
- Hardcode 12 monthly EUR/USD values (Apr 2025 - Mar 2026, approximate from public data): [0.91, 0.92, 0.93, 0.92, 0.91, 0.90, 0.91, 0.92, 0.93, 0.92, 0.91, 0.92]
- Hardcode 12 monthly USD/CNY values: [7.21, 7.24, 7.20, 7.18, 7.22, 7.25, 7.23, 7.20, 7.24, 7.26, 7.22, 7.24]
- Store chart configs in _chartData for lazy rendering via getChartConfigs()
- Two line charts: EUR/USD and USD/CNY with month labels (Apr, May, Jun, ... Mar)
- Use CHART_COLORS.economy for line colors

**Context tiles (block 3 -- tiles):**
- Tile 1: Strongest currency (CHF or similar note)
- Tile 2: Most volatile (TRY/ARS)
- Tile 3: Number of tracked currencies

**Hyperinflation highlight (block 4 -- explanation):**
- List hyperinflation countries: Venezuela (VES), Zimbabwe (ZWL), Argentina (ARS), Turkey (TRY), Lebanon (LBP)
- For each: country name, currency code, approximate annual inflation rate
- Use colored badges (red/orange) to indicate severity
- Build with DOMUtils.create, flex layout with gap

**Comparison and Sources blocks (blocks 5-6):**
- Explanation text from i18n
- Comparison text from i18n
- Source: Open ER API link

**cleanup():** Clear _chartData = null (no intervals needed for currencies)
  </action>
  <verify>
    <automated>node -e "import('./detail/topics/currencies.js').then(m => { const ok = m.meta && m.meta.id === 'currencies' && typeof m.render === 'function' && typeof m.getChartConfigs === 'function' && typeof m.cleanup === 'function'; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>detail/?topic=currencies renders: exchange rate hero with badge, working currency converter (input + selects + result), 12-month EUR/USD and USD/CNY line charts, hyperinflation countries section, explanation and sources. No console errors.</done>
</task>

</tasks>

<verification>
- Open detail/?topic=currencies in browser -- page loads without console errors
- Exchange rates display with correct tier badge
- Currency converter: enter 100, select EUR->JPY, result shows correct conversion
- Currency converter: enter non-numeric text, result shows "--"
- 12-month charts render with month labels on x-axis
- Hyperinflation section shows 5 countries with colored badges
- Switch language (?lang=en) -- all text translates
</verification>

<success_criteria>
1. All 5 Phase 7 topics have complete DE/EN i18n keys (approximately 80 keys total)
2. Static fallback values populated for currencies, science, internet, space, weather
3. Currencies topic page fully functional with converter, charts, and hyperinflation highlight
4. Module exports meta, render, getChartConfigs, cleanup per topic contract
</success_criteria>

<output>
After completion, create `.planning/phases/07-economy-progress-weather/07-01-SUMMARY.md`
</output>
